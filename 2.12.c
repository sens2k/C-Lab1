#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
/*Свечин Валентин
  ВПИ22
  Лаба - 1, задание - 2.12
*/

int main(int argc, char* argv[])
{
	/*
		1) c - s / i - тип short
		2) u * 3 - 3.0 * u - i - тип float
		3) u - us * i - тип int
		4) ( sc + d ) * ld - тип double
		5) (5 * lng - 'a' ) * ( s + u / 2 ) - тип long
		6) ( f + 3 ) / ( 2.5f - s * 3.14) - тип float
	*/
	char c; short s; int i; unsigned u; signed char sc;
	float f; double d; long lng; unsigned short us; long double ld;
}