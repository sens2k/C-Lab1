#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
/*Свечин Валентин
  ВПИ22
  Лаба - 1, задание - 2.11	
*/

int main(int argc, char* argv[])
{
	/*
		1) !( a>b ) эквивалент  a<=b
		2) ! ( 2*a == b+4 ) эквивалент 2*a != b+4
		3) ! ( a<b && c<d ) эквивалент	a>=b || c>=d
		4) ! ( a<2 || a>5) эквивалент a>=2 && a<=5
		5) ! ( a<1 || b<2 && c<3 ) эквивалент a>=1 && b>=2 || c>=3
	*/

}